sum(a: int, b: int): int {
  return a + b;
}

sum(a: int[], b: int[], length: int): void {
  for (int i = 0; i < length; i++)
    a[i] += b[i];
}

gt(a: int, b: int): bool {
  return a > b;
}

static mul(a: int, b: int): int {
 return a * b;
}
